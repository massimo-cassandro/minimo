Files in this directory are generated from the `*.minimo.tokens.mjs` sources located in the `design-tokens/_src` and `src` directories.

These files can be used with specific tools such as Penpot, Figma (via plugins), Token Studio, or as an alternative to the .mjs source files.
