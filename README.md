# minimo

Very light-size framework

...and some install [snippets](snippets/snippets.md)

## Install

```bash
npm i @massimo-cassandro/minimo
```

> Note: **minimo** is designed to be used with WebPack using the configuration you can find in the [boilerplate folder](./boilerplate/) of this repository. If changes are made (for example, to how SVG files are imported), things may not work as expected
