/*

onKeyDown={ e => {
        if(e.code === 'Enter'){
          updateUrl(1, e.target.value);
        }
      }}
      onInput={ e => { // per il pulsante di reset
        if(e.target.value === '') {
          updateUrl(1, '');
        }
      }}
 */


// TODO defaultSorting => conversione nel formato datatable se jqDatatableCompliant = true
// TODO customSortFunction
