completare
aggionare la documentazione e aggiungere i test a demo (vedi vecchia versione in `TODO charts/test`)

implementare tooltip nativo nei file svg, ora viene aggiunto un data-title, ma non è gestito


aggiungere export specifico in package.json
